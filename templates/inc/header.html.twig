<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container-fluid">   
            <a class="navbar-brand" href="{{ path('homepage')}}">MyBooking</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('homepage')}}">Accueil</a>
                    </li>
                </ul>

                <ul class="navbar-nav ml-auto">
                    {% if app.user %}
                        <li class="dropdown">
                            <a href="#" class="btn btn-primary dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false" id="dropdownMenuButton">
                                <img src="{{ app.user.avatar }}" alt="Avatar de {{ app.user.fullName }}" class="avatar avatar-small">
                                {{ app.user.fullName}}
                            </a>
                            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuButton">
                                <a class="dropdown-item" href="{{ path('ads_create') }}">Créer une annonce</a>
                                <div class="dropdown-divider"></div>
                                <a class="dropdown-item" href="{{ path('account_home') }}">Mon compte</a>
                                <a class="dropdown-item" href="{{ path('account_profile') }}">Modifier mon profil</a>
                                <div class="dropdown-divider"></div>
                                <a class="dropdown-item" href="{{ path('account_logout') }}">Déconnexion</a>
                                
                            </div>
                        </li>
                    {% else %}
                            <li>
                                <a class="dropdown-item" href="{{ path('account_register') }}">Inscription</a>
                            </li>
                            <li>
                                <a class="dropdown-item" href="{{ path('account_login') }}">Connexion</a>
                            </li>
                    {% endif %}
                </ul>
            </div>
    </div>
</nav>